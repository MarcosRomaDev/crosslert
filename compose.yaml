services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=mydatabase'
      - 'POSTGRES_PASSWORD=${BBDD_PASSWORD}'
      - 'POSTGRES_USER=${BBDD_USER}'
    ports:
      - '5433:5432'
